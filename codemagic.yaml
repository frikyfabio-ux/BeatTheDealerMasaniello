workflows:
  android-workflow:
    name: Build APK Masaniello
    max_build_duration: 60
    scripts:
      - gradle clean
      - gradle assembleRelease
    artifacts:
      - app/build/outputs/apk/release/app-release.apk
